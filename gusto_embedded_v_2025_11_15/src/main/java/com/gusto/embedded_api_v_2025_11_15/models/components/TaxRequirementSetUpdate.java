/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2025_11_15.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.gusto.embedded_api_v_2025_11_15.utils.Utils;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.List;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;


public class TaxRequirementSetUpdate {
    /**
     * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with
     * the same `key` and different `effective_from` values.
     */
    @JsonProperty("key")
    private String key;

    /**
     * One of the two-letter state abbreviations for the fifty United States and the District of Columbia
     * (DC)
     */
    @JsonProperty("state")
    private String state;

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are
     * effective dated, while others are not. Multiple requirement sets for the same state/key can/will
     * exist with unique effective dates.
     * 
     * <p>If a requirement set is has an `effective_from` value, all requirement sets with the same key will
     * also have an `effective_from` value.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("effective_from")
    private JsonNullable<String> effectiveFrom;


    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("requirements")
    private Optional<? extends List<Requirements>> requirements;

    @JsonCreator
    public TaxRequirementSetUpdate(
            @JsonProperty("key") String key,
            @JsonProperty("state") String state,
            @JsonProperty("effective_from") JsonNullable<String> effectiveFrom,
            @JsonProperty("requirements") Optional<? extends List<Requirements>> requirements) {
        Utils.checkNotNull(key, "key");
        Utils.checkNotNull(state, "state");
        Utils.checkNotNull(effectiveFrom, "effectiveFrom");
        Utils.checkNotNull(requirements, "requirements");
        this.key = key;
        this.state = state;
        this.effectiveFrom = effectiveFrom;
        this.requirements = requirements;
    }
    
    public TaxRequirementSetUpdate(
            String key,
            String state) {
        this(key, state, JsonNullable.undefined(),
            Optional.empty());
    }

    /**
     * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with
     * the same `key` and different `effective_from` values.
     */
    @JsonIgnore
    public String key() {
        return key;
    }

    /**
     * One of the two-letter state abbreviations for the fifty United States and the District of Columbia
     * (DC)
     */
    @JsonIgnore
    public String state() {
        return state;
    }

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are
     * effective dated, while others are not. Multiple requirement sets for the same state/key can/will
     * exist with unique effective dates.
     * 
     * <p>If a requirement set is has an `effective_from` value, all requirement sets with the same key will
     * also have an `effective_from` value.
     */
    @JsonIgnore
    public JsonNullable<String> effectiveFrom() {
        return effectiveFrom;
    }

    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<Requirements>> requirements() {
        return (Optional<List<Requirements>>) requirements;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with
     * the same `key` and different `effective_from` values.
     */
    public TaxRequirementSetUpdate withKey(String key) {
        Utils.checkNotNull(key, "key");
        this.key = key;
        return this;
    }

    /**
     * One of the two-letter state abbreviations for the fifty United States and the District of Columbia
     * (DC)
     */
    public TaxRequirementSetUpdate withState(String state) {
        Utils.checkNotNull(state, "state");
        this.state = state;
        return this;
    }

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are
     * effective dated, while others are not. Multiple requirement sets for the same state/key can/will
     * exist with unique effective dates.
     * 
     * <p>If a requirement set is has an `effective_from` value, all requirement sets with the same key will
     * also have an `effective_from` value.
     */
    public TaxRequirementSetUpdate withEffectiveFrom(String effectiveFrom) {
        Utils.checkNotNull(effectiveFrom, "effectiveFrom");
        this.effectiveFrom = JsonNullable.of(effectiveFrom);
        return this;
    }

    /**
     * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are
     * effective dated, while others are not. Multiple requirement sets for the same state/key can/will
     * exist with unique effective dates.
     * 
     * <p>If a requirement set is has an `effective_from` value, all requirement sets with the same key will
     * also have an `effective_from` value.
     */
    public TaxRequirementSetUpdate withEffectiveFrom(JsonNullable<String> effectiveFrom) {
        Utils.checkNotNull(effectiveFrom, "effectiveFrom");
        this.effectiveFrom = effectiveFrom;
        return this;
    }

    public TaxRequirementSetUpdate withRequirements(List<Requirements> requirements) {
        Utils.checkNotNull(requirements, "requirements");
        this.requirements = Optional.ofNullable(requirements);
        return this;
    }


    public TaxRequirementSetUpdate withRequirements(Optional<? extends List<Requirements>> requirements) {
        Utils.checkNotNull(requirements, "requirements");
        this.requirements = requirements;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        TaxRequirementSetUpdate other = (TaxRequirementSetUpdate) o;
        return 
            Utils.enhancedDeepEquals(this.key, other.key) &&
            Utils.enhancedDeepEquals(this.state, other.state) &&
            Utils.enhancedDeepEquals(this.effectiveFrom, other.effectiveFrom) &&
            Utils.enhancedDeepEquals(this.requirements, other.requirements);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            key, state, effectiveFrom,
            requirements);
    }
    
    @Override
    public String toString() {
        return Utils.toString(TaxRequirementSetUpdate.class,
                "key", key,
                "state", state,
                "effectiveFrom", effectiveFrom,
                "requirements", requirements);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String key;

        private String state;

        private JsonNullable<String> effectiveFrom = JsonNullable.undefined();

        private Optional<? extends List<Requirements>> requirements = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * An identifier for a set of requirements. A list of requirement sets can contain multiple sets with
         * the same `key` and different `effective_from` values.
         */
        public Builder key(String key) {
            Utils.checkNotNull(key, "key");
            this.key = key;
            return this;
        }


        /**
         * One of the two-letter state abbreviations for the fifty United States and the District of Columbia
         * (DC)
         */
        public Builder state(String state) {
            Utils.checkNotNull(state, "state");
            this.state = state;
            return this;
        }


        /**
         * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are
         * effective dated, while others are not. Multiple requirement sets for the same state/key can/will
         * exist with unique effective dates.
         * 
         * <p>If a requirement set is has an `effective_from` value, all requirement sets with the same key will
         * also have an `effective_from` value.
         */
        public Builder effectiveFrom(String effectiveFrom) {
            Utils.checkNotNull(effectiveFrom, "effectiveFrom");
            this.effectiveFrom = JsonNullable.of(effectiveFrom);
            return this;
        }

        /**
         * An ISO 8601 formatted date representing the date values became effective. Some requirement sets are
         * effective dated, while others are not. Multiple requirement sets for the same state/key can/will
         * exist with unique effective dates.
         * 
         * <p>If a requirement set is has an `effective_from` value, all requirement sets with the same key will
         * also have an `effective_from` value.
         */
        public Builder effectiveFrom(JsonNullable<String> effectiveFrom) {
            Utils.checkNotNull(effectiveFrom, "effectiveFrom");
            this.effectiveFrom = effectiveFrom;
            return this;
        }


        public Builder requirements(List<Requirements> requirements) {
            Utils.checkNotNull(requirements, "requirements");
            this.requirements = Optional.ofNullable(requirements);
            return this;
        }

        public Builder requirements(Optional<? extends List<Requirements>> requirements) {
            Utils.checkNotNull(requirements, "requirements");
            this.requirements = requirements;
            return this;
        }

        public TaxRequirementSetUpdate build() {

            return new TaxRequirementSetUpdate(
                key, state, effectiveFrom,
                requirements);
        }

    }
}
