/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2025_11_15.models.components;

import com.fasterxml.jackson.annotation.JsonValue;
import java.lang.String;
import java.util.Objects;
import java.util.Optional;

/**
 * PayScheduleFrequency
 * 
 * <p>The frequency that employees on this pay schedule are paid with Gusto.
 * 
 * <p>READ-ONLY in responses. Possible values:
 * 
 * <p>- `Every week`: Employees are paid weekly.
 * - `Every other week`: Employees are paid bi-weekly (every two weeks).
 * - `Twice per month`: Employees are paid on two fixed days each month (e.g. 1st and 15th); use day_1
 * and day_2.
 * - `Monthly`: Employees are paid once per month; use day_1 for the pay day.
 * - `Quarterly`: Employees are paid every three months.
 * - `Annually`: Employees are paid once per year.
 */
public enum PayScheduleFrequency {
    EVERY_WEEK("Every week"),
    EVERY_OTHER_WEEK("Every other week"),
    TWICE_PER_MONTH("Twice per month"),
    MONTHLY("Monthly"),
    QUARTERLY("Quarterly"),
    ANNUALLY("Annually");

    @JsonValue
    private final String value;

    PayScheduleFrequency(String value) {
        this.value = value;
    }
    
    public String value() {
        return value;
    }
    
    public static Optional<PayScheduleFrequency> fromValue(String value) {
        for (PayScheduleFrequency o: PayScheduleFrequency.values()) {
            if (Objects.deepEquals(o.value, value)) {
                return Optional.of(o);
            }
        }
        return Optional.empty();
    }
}

