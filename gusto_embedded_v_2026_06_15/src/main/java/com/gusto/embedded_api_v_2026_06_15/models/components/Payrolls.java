/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api_v_2026_06_15.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.gusto.embedded_api_v_2026_06_15.utils.Utils;
import java.lang.Boolean;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;


public class Payrolls {
    /**
     * UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls`
     * API has not yet created a payroll record). Once a payroll is created, subsequent digest requests
     * will include the real `payroll_uuid`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("payroll_uuid")
    private JsonNullable<String> payrollUuid;

    /**
     * The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`,
     * `correction`).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("payroll_type")
    private Optional<String> payrollType;

    /**
     * Partner-facing display title for this payroll (e.g. "Run biweekly payroll").
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("display_title")
    private Optional<String> displayTitle;

    /**
     * Whether the company has auto-payroll enabled for this pay schedule.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("auto_payroll")
    private Optional<Boolean> autoPayroll;

    /**
     * The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`,
     * `failed`).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("status")
    private Optional<String> status;


    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("pay_period")
    private Optional<? extends PayrollDigestResultsPayPeriod> payPeriod;


    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("pay_schedule")
    private JsonNullable<? extends PaySchedule> paySchedule;

    /**
     * Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the
     * partner edited hours/earnings after the last calculation).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("totals")
    private JsonNullable<? extends PayrollDigestResultsTotals> totals;

    @JsonCreator
    public Payrolls(
            @JsonProperty("payroll_uuid") JsonNullable<String> payrollUuid,
            @JsonProperty("payroll_type") Optional<String> payrollType,
            @JsonProperty("display_title") Optional<String> displayTitle,
            @JsonProperty("auto_payroll") Optional<Boolean> autoPayroll,
            @JsonProperty("status") Optional<String> status,
            @JsonProperty("pay_period") Optional<? extends PayrollDigestResultsPayPeriod> payPeriod,
            @JsonProperty("pay_schedule") JsonNullable<? extends PaySchedule> paySchedule,
            @JsonProperty("totals") JsonNullable<? extends PayrollDigestResultsTotals> totals) {
        Utils.checkNotNull(payrollUuid, "payrollUuid");
        Utils.checkNotNull(payrollType, "payrollType");
        Utils.checkNotNull(displayTitle, "displayTitle");
        Utils.checkNotNull(autoPayroll, "autoPayroll");
        Utils.checkNotNull(status, "status");
        Utils.checkNotNull(payPeriod, "payPeriod");
        Utils.checkNotNull(paySchedule, "paySchedule");
        Utils.checkNotNull(totals, "totals");
        this.payrollUuid = payrollUuid;
        this.payrollType = payrollType;
        this.displayTitle = displayTitle;
        this.autoPayroll = autoPayroll;
        this.status = status;
        this.payPeriod = payPeriod;
        this.paySchedule = paySchedule;
        this.totals = totals;
    }
    
    public Payrolls() {
        this(JsonNullable.undefined(), Optional.empty(), Optional.empty(),
            Optional.empty(), Optional.empty(), Optional.empty(),
            JsonNullable.undefined(), JsonNullable.undefined());
    }

    /**
     * UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls`
     * API has not yet created a payroll record). Once a payroll is created, subsequent digest requests
     * will include the real `payroll_uuid`.
     */
    @JsonIgnore
    public JsonNullable<String> payrollUuid() {
        return payrollUuid;
    }

    /**
     * The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`,
     * `correction`).
     */
    @JsonIgnore
    public Optional<String> payrollType() {
        return payrollType;
    }

    /**
     * Partner-facing display title for this payroll (e.g. "Run biweekly payroll").
     */
    @JsonIgnore
    public Optional<String> displayTitle() {
        return displayTitle;
    }

    /**
     * Whether the company has auto-payroll enabled for this pay schedule.
     */
    @JsonIgnore
    public Optional<Boolean> autoPayroll() {
        return autoPayroll;
    }

    /**
     * The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`,
     * `failed`).
     */
    @JsonIgnore
    public Optional<String> status() {
        return status;
    }

    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<PayrollDigestResultsPayPeriod> payPeriod() {
        return (Optional<PayrollDigestResultsPayPeriod>) payPeriod;
    }

    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<PaySchedule> paySchedule() {
        return (JsonNullable<PaySchedule>) paySchedule;
    }

    /**
     * Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the
     * partner edited hours/earnings after the last calculation).
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public JsonNullable<PayrollDigestResultsTotals> totals() {
        return (JsonNullable<PayrollDigestResultsTotals>) totals;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls`
     * API has not yet created a payroll record). Once a payroll is created, subsequent digest requests
     * will include the real `payroll_uuid`.
     */
    public Payrolls withPayrollUuid(String payrollUuid) {
        Utils.checkNotNull(payrollUuid, "payrollUuid");
        this.payrollUuid = JsonNullable.of(payrollUuid);
        return this;
    }

    /**
     * UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls`
     * API has not yet created a payroll record). Once a payroll is created, subsequent digest requests
     * will include the real `payroll_uuid`.
     */
    public Payrolls withPayrollUuid(JsonNullable<String> payrollUuid) {
        Utils.checkNotNull(payrollUuid, "payrollUuid");
        this.payrollUuid = payrollUuid;
        return this;
    }

    /**
     * The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`,
     * `correction`).
     */
    public Payrolls withPayrollType(String payrollType) {
        Utils.checkNotNull(payrollType, "payrollType");
        this.payrollType = Optional.ofNullable(payrollType);
        return this;
    }


    /**
     * The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`,
     * `correction`).
     */
    public Payrolls withPayrollType(Optional<String> payrollType) {
        Utils.checkNotNull(payrollType, "payrollType");
        this.payrollType = payrollType;
        return this;
    }

    /**
     * Partner-facing display title for this payroll (e.g. "Run biweekly payroll").
     */
    public Payrolls withDisplayTitle(String displayTitle) {
        Utils.checkNotNull(displayTitle, "displayTitle");
        this.displayTitle = Optional.ofNullable(displayTitle);
        return this;
    }


    /**
     * Partner-facing display title for this payroll (e.g. "Run biweekly payroll").
     */
    public Payrolls withDisplayTitle(Optional<String> displayTitle) {
        Utils.checkNotNull(displayTitle, "displayTitle");
        this.displayTitle = displayTitle;
        return this;
    }

    /**
     * Whether the company has auto-payroll enabled for this pay schedule.
     */
    public Payrolls withAutoPayroll(boolean autoPayroll) {
        Utils.checkNotNull(autoPayroll, "autoPayroll");
        this.autoPayroll = Optional.ofNullable(autoPayroll);
        return this;
    }


    /**
     * Whether the company has auto-payroll enabled for this pay schedule.
     */
    public Payrolls withAutoPayroll(Optional<Boolean> autoPayroll) {
        Utils.checkNotNull(autoPayroll, "autoPayroll");
        this.autoPayroll = autoPayroll;
        return this;
    }

    /**
     * The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`,
     * `failed`).
     */
    public Payrolls withStatus(String status) {
        Utils.checkNotNull(status, "status");
        this.status = Optional.ofNullable(status);
        return this;
    }


    /**
     * The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`,
     * `failed`).
     */
    public Payrolls withStatus(Optional<String> status) {
        Utils.checkNotNull(status, "status");
        this.status = status;
        return this;
    }

    public Payrolls withPayPeriod(PayrollDigestResultsPayPeriod payPeriod) {
        Utils.checkNotNull(payPeriod, "payPeriod");
        this.payPeriod = Optional.ofNullable(payPeriod);
        return this;
    }


    public Payrolls withPayPeriod(Optional<? extends PayrollDigestResultsPayPeriod> payPeriod) {
        Utils.checkNotNull(payPeriod, "payPeriod");
        this.payPeriod = payPeriod;
        return this;
    }

    public Payrolls withPaySchedule(PaySchedule paySchedule) {
        Utils.checkNotNull(paySchedule, "paySchedule");
        this.paySchedule = JsonNullable.of(paySchedule);
        return this;
    }

    public Payrolls withPaySchedule(JsonNullable<? extends PaySchedule> paySchedule) {
        Utils.checkNotNull(paySchedule, "paySchedule");
        this.paySchedule = paySchedule;
        return this;
    }

    /**
     * Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the
     * partner edited hours/earnings after the last calculation).
     */
    public Payrolls withTotals(PayrollDigestResultsTotals totals) {
        Utils.checkNotNull(totals, "totals");
        this.totals = JsonNullable.of(totals);
        return this;
    }

    /**
     * Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the
     * partner edited hours/earnings after the last calculation).
     */
    public Payrolls withTotals(JsonNullable<? extends PayrollDigestResultsTotals> totals) {
        Utils.checkNotNull(totals, "totals");
        this.totals = totals;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        Payrolls other = (Payrolls) o;
        return 
            Utils.enhancedDeepEquals(this.payrollUuid, other.payrollUuid) &&
            Utils.enhancedDeepEquals(this.payrollType, other.payrollType) &&
            Utils.enhancedDeepEquals(this.displayTitle, other.displayTitle) &&
            Utils.enhancedDeepEquals(this.autoPayroll, other.autoPayroll) &&
            Utils.enhancedDeepEquals(this.status, other.status) &&
            Utils.enhancedDeepEquals(this.payPeriod, other.payPeriod) &&
            Utils.enhancedDeepEquals(this.paySchedule, other.paySchedule) &&
            Utils.enhancedDeepEquals(this.totals, other.totals);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            payrollUuid, payrollType, displayTitle,
            autoPayroll, status, payPeriod,
            paySchedule, totals);
    }
    
    @Override
    public String toString() {
        return Utils.toString(Payrolls.class,
                "payrollUuid", payrollUuid,
                "payrollType", payrollType,
                "displayTitle", displayTitle,
                "autoPayroll", autoPayroll,
                "status", status,
                "payPeriod", payPeriod,
                "paySchedule", paySchedule,
                "totals", totals);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private JsonNullable<String> payrollUuid = JsonNullable.undefined();

        private Optional<String> payrollType = Optional.empty();

        private Optional<String> displayTitle = Optional.empty();

        private Optional<Boolean> autoPayroll = Optional.empty();

        private Optional<String> status = Optional.empty();

        private Optional<? extends PayrollDigestResultsPayPeriod> payPeriod = Optional.empty();

        private JsonNullable<? extends PaySchedule> paySchedule = JsonNullable.undefined();

        private JsonNullable<? extends PayrollDigestResultsTotals> totals = JsonNullable.undefined();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls`
         * API has not yet created a payroll record). Once a payroll is created, subsequent digest requests
         * will include the real `payroll_uuid`.
         */
        public Builder payrollUuid(String payrollUuid) {
            Utils.checkNotNull(payrollUuid, "payrollUuid");
            this.payrollUuid = JsonNullable.of(payrollUuid);
            return this;
        }

        /**
         * UUID of the payroll. `null` for upcoming pay periods that have not been started yet (the `payrolls`
         * API has not yet created a payroll record). Once a payroll is created, subsequent digest requests
         * will include the real `payroll_uuid`.
         */
        public Builder payrollUuid(JsonNullable<String> payrollUuid) {
            Utils.checkNotNull(payrollUuid, "payrollUuid");
            this.payrollUuid = payrollUuid;
            return this;
        }


        /**
         * The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`,
         * `correction`).
         */
        public Builder payrollType(String payrollType) {
            Utils.checkNotNull(payrollType, "payrollType");
            this.payrollType = Optional.ofNullable(payrollType);
            return this;
        }

        /**
         * The type of payroll (e.g. `regular`, `new_hire`, `termination`, `transition`, `bonus`,
         * `correction`).
         */
        public Builder payrollType(Optional<String> payrollType) {
            Utils.checkNotNull(payrollType, "payrollType");
            this.payrollType = payrollType;
            return this;
        }


        /**
         * Partner-facing display title for this payroll (e.g. "Run biweekly payroll").
         */
        public Builder displayTitle(String displayTitle) {
            Utils.checkNotNull(displayTitle, "displayTitle");
            this.displayTitle = Optional.ofNullable(displayTitle);
            return this;
        }

        /**
         * Partner-facing display title for this payroll (e.g. "Run biweekly payroll").
         */
        public Builder displayTitle(Optional<String> displayTitle) {
            Utils.checkNotNull(displayTitle, "displayTitle");
            this.displayTitle = displayTitle;
            return this;
        }


        /**
         * Whether the company has auto-payroll enabled for this pay schedule.
         */
        public Builder autoPayroll(boolean autoPayroll) {
            Utils.checkNotNull(autoPayroll, "autoPayroll");
            this.autoPayroll = Optional.ofNullable(autoPayroll);
            return this;
        }

        /**
         * Whether the company has auto-payroll enabled for this pay schedule.
         */
        public Builder autoPayroll(Optional<Boolean> autoPayroll) {
            Utils.checkNotNull(autoPayroll, "autoPayroll");
            this.autoPayroll = autoPayroll;
            return this;
        }


        /**
         * The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`,
         * `failed`).
         */
        public Builder status(String status) {
            Utils.checkNotNull(status, "status");
            this.status = Optional.ofNullable(status);
            return this;
        }

        /**
         * The lifecycle status of the payroll (e.g. `ready_to_start`, `in_progress`, `submitted`, `completed`,
         * `failed`).
         */
        public Builder status(Optional<String> status) {
            Utils.checkNotNull(status, "status");
            this.status = status;
            return this;
        }


        public Builder payPeriod(PayrollDigestResultsPayPeriod payPeriod) {
            Utils.checkNotNull(payPeriod, "payPeriod");
            this.payPeriod = Optional.ofNullable(payPeriod);
            return this;
        }

        public Builder payPeriod(Optional<? extends PayrollDigestResultsPayPeriod> payPeriod) {
            Utils.checkNotNull(payPeriod, "payPeriod");
            this.payPeriod = payPeriod;
            return this;
        }


        public Builder paySchedule(PaySchedule paySchedule) {
            Utils.checkNotNull(paySchedule, "paySchedule");
            this.paySchedule = JsonNullable.of(paySchedule);
            return this;
        }

        public Builder paySchedule(JsonNullable<? extends PaySchedule> paySchedule) {
            Utils.checkNotNull(paySchedule, "paySchedule");
            this.paySchedule = paySchedule;
            return this;
        }


        /**
         * Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the
         * partner edited hours/earnings after the last calculation).
         */
        public Builder totals(PayrollDigestResultsTotals totals) {
            Utils.checkNotNull(totals, "totals");
            this.totals = JsonNullable.of(totals);
            return this;
        }

        /**
         * Pay totals. `null` when the payroll has not been calculated, or when the calculation is stale (the
         * partner edited hours/earnings after the last calculation).
         */
        public Builder totals(JsonNullable<? extends PayrollDigestResultsTotals> totals) {
            Utils.checkNotNull(totals, "totals");
            this.totals = totals;
            return this;
        }

        public Payrolls build() {

            return new Payrolls(
                payrollUuid, payrollType, displayTitle,
                autoPayroll, status, payPeriod,
                paySchedule, totals);
        }

    }
}
