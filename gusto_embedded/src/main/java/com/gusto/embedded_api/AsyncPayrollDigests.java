/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api;

import static com.gusto.embedded_api.operations.Operations.AsyncRequestOperation;

import com.gusto.embedded_api.models.operations.GetV1PayrollDigestsPayrollDigestUuidHeaderXGustoAPIVersion;
import com.gusto.embedded_api.models.operations.GetV1PayrollDigestsPayrollDigestUuidRequest;
import com.gusto.embedded_api.models.operations.GetV1PayrollDigestsPayrollDigestUuidSecurity;
import com.gusto.embedded_api.models.operations.PostV1PayrollDigestsHeaderXGustoAPIVersion;
import com.gusto.embedded_api.models.operations.PostV1PayrollDigestsRequest;
import com.gusto.embedded_api.models.operations.PostV1PayrollDigestsRequestBody;
import com.gusto.embedded_api.models.operations.PostV1PayrollDigestsSecurity;
import com.gusto.embedded_api.models.operations.async.GetV1PayrollDigestsPayrollDigestUuidRequestBuilder;
import com.gusto.embedded_api.models.operations.async.GetV1PayrollDigestsPayrollDigestUuidResponse;
import com.gusto.embedded_api.models.operations.async.PostV1PayrollDigestsRequestBuilder;
import com.gusto.embedded_api.models.operations.async.PostV1PayrollDigestsResponse;
import com.gusto.embedded_api.operations.GetV1PayrollDigestsPayrollDigestUuid;
import com.gusto.embedded_api.operations.PostV1PayrollDigests;
import com.gusto.embedded_api.utils.Headers;
import java.lang.String;
import java.util.Optional;
import java.util.concurrent.CompletableFuture;


public class AsyncPayrollDigests {
    private static final Headers _headers = Headers.EMPTY;
    private final SDKConfiguration sdkConfiguration;
    private final PayrollDigests syncSDK;

    AsyncPayrollDigests(PayrollDigests syncSDK, SDKConfiguration sdkConfiguration) {
        this.sdkConfiguration = sdkConfiguration;
        this.syncSDK = syncSDK;
    }

    /**
     * Switches to the sync SDK.
     * 
     * @return The sync SDK
     */
    public PayrollDigests sync() {
        return syncSDK;
    }


    /**
     * Create a payroll digest batch
     * 
     * <p>Triggers an asynchronous computation of payroll digest data (statuses, blockers, pay periods,
     * totals) across up to 25 companies that the partner is mapped to.
     * 
     * <p>The batch is processed asynchronously. Use the returned batch UUID to poll `GET
     * /v1/payroll_digests/{payroll_digest_uuid}` for status and results.
     * 
     * <p>Idempotency is scoped per `(partner, idempotency_key)`. A duplicate POST with the same
     * `idempotency_key` returns a 409 Conflict referencing the existing batch UUID — no duplicate
     * computation occurs.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_digests:write`
     * 
     * @return The async call builder
     */
    public PostV1PayrollDigestsRequestBuilder postV1PayrollDigests() {
        return new PostV1PayrollDigestsRequestBuilder(sdkConfiguration);
    }

    /**
     * Create a payroll digest batch
     * 
     * <p>Triggers an asynchronous computation of payroll digest data (statuses, blockers, pay periods,
     * totals) across up to 25 companies that the partner is mapped to.
     * 
     * <p>The batch is processed asynchronously. Use the returned batch UUID to poll `GET
     * /v1/payroll_digests/{payroll_digest_uuid}` for status and results.
     * 
     * <p>Idempotency is scoped per `(partner, idempotency_key)`. A duplicate POST with the same
     * `idempotency_key` returns a 409 Conflict referencing the existing batch UUID — no duplicate
     * computation occurs.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_digests:write`
     * 
     * @param security The security details to use for authentication.
     * @param requestBody 
     * @return {@code CompletableFuture<PostV1PayrollDigestsResponse>} - The async response
     */
    public CompletableFuture<PostV1PayrollDigestsResponse> postV1PayrollDigests(PostV1PayrollDigestsSecurity security, PostV1PayrollDigestsRequestBody requestBody) {
        return postV1PayrollDigests(security, Optional.empty(), requestBody);
    }

    /**
     * Create a payroll digest batch
     * 
     * <p>Triggers an asynchronous computation of payroll digest data (statuses, blockers, pay periods,
     * totals) across up to 25 companies that the partner is mapped to.
     * 
     * <p>The batch is processed asynchronously. Use the returned batch UUID to poll `GET
     * /v1/payroll_digests/{payroll_digest_uuid}` for status and results.
     * 
     * <p>Idempotency is scoped per `(partner, idempotency_key)`. A duplicate POST with the same
     * `idempotency_key` returns a 409 Conflict referencing the existing batch UUID — no duplicate
     * computation occurs.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_digests:write`
     * 
     * @param security The security details to use for authentication.
     * @param xGustoAPIVersion Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
     * @param requestBody 
     * @return {@code CompletableFuture<PostV1PayrollDigestsResponse>} - The async response
     */
    public CompletableFuture<PostV1PayrollDigestsResponse> postV1PayrollDigests(
            PostV1PayrollDigestsSecurity security, Optional<? extends PostV1PayrollDigestsHeaderXGustoAPIVersion> xGustoAPIVersion,
            PostV1PayrollDigestsRequestBody requestBody) {
        PostV1PayrollDigestsRequest request =
            PostV1PayrollDigestsRequest
                .builder()
                .xGustoAPIVersion(xGustoAPIVersion)
                .requestBody(requestBody)
                .build();
        AsyncRequestOperation<PostV1PayrollDigestsRequest, PostV1PayrollDigestsResponse> operation
              = new PostV1PayrollDigests.Async(sdkConfiguration, security, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }


    /**
     * Get a payroll digest batch
     * 
     * <p>Returns the status and results of a payroll digest batch.
     * 
     * <p>Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once
     * terminal, the response includes the full `results` array (one entry per attempted company, each with
     * its own per-company `status` — `success`, `partial_success`, or `failed`) and the `exclusions` array
     * (one entry per company that could not be looked up or processed).
     * 
     * <p>Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is
     * distinct from the per-company `status` returned inside `results[]` and `exclusions[]`. A `completed`
     * batch does not imply every company succeeded — inspect the arrays for per-company outcomes.
     * 
     * <p>Results are stored in Redis with a short TTL after completion. If the partner polls after results
     * have expired, this endpoint returns 410 Gone — partners should re-submit a new batch to fetch fresh
     * data.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_digests:read`
     * 
     * @return The async call builder
     */
    public GetV1PayrollDigestsPayrollDigestUuidRequestBuilder getV1PayrollDigestsPayrollDigestUuid() {
        return new GetV1PayrollDigestsPayrollDigestUuidRequestBuilder(sdkConfiguration);
    }

    /**
     * Get a payroll digest batch
     * 
     * <p>Returns the status and results of a payroll digest batch.
     * 
     * <p>Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once
     * terminal, the response includes the full `results` array (one entry per attempted company, each with
     * its own per-company `status` — `success`, `partial_success`, or `failed`) and the `exclusions` array
     * (one entry per company that could not be looked up or processed).
     * 
     * <p>Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is
     * distinct from the per-company `status` returned inside `results[]` and `exclusions[]`. A `completed`
     * batch does not imply every company succeeded — inspect the arrays for per-company outcomes.
     * 
     * <p>Results are stored in Redis with a short TTL after completion. If the partner polls after results
     * have expired, this endpoint returns 410 Gone — partners should re-submit a new batch to fetch fresh
     * data.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_digests:read`
     * 
     * @param security The security details to use for authentication.
     * @param payrollDigestUuid The UUID of the payroll digest batch returned by `POST /v1/payroll_digests`.
     * @return {@code CompletableFuture<GetV1PayrollDigestsPayrollDigestUuidResponse>} - The async response
     */
    public CompletableFuture<GetV1PayrollDigestsPayrollDigestUuidResponse> getV1PayrollDigestsPayrollDigestUuid(GetV1PayrollDigestsPayrollDigestUuidSecurity security, String payrollDigestUuid) {
        return getV1PayrollDigestsPayrollDigestUuid(security, payrollDigestUuid, Optional.empty());
    }

    /**
     * Get a payroll digest batch
     * 
     * <p>Returns the status and results of a payroll digest batch.
     * 
     * <p>Poll this endpoint until the batch `status` reaches a terminal value (`completed` or `failed`). Once
     * terminal, the response includes the full `results` array (one entry per attempted company, each with
     * its own per-company `status` — `success`, `partial_success`, or `failed`) and the `exclusions` array
     * (one entry per company that could not be looked up or processed).
     * 
     * <p>Note that the top-level batch `status` (`pending` / `processing` / `completed` / `failed`) is
     * distinct from the per-company `status` returned inside `results[]` and `exclusions[]`. A `completed`
     * batch does not imply every company succeeded — inspect the arrays for per-company outcomes.
     * 
     * <p>Results are stored in Redis with a short TTL after completion. If the partner polls after results
     * have expired, this endpoint returns 410 Gone — partners should re-submit a new batch to fetch fresh
     * data.
     * 
     * <p>📘 System Access Authentication
     * 
     * <p>This endpoint uses the [Bearer Auth scheme with the system-level access token in the HTTP
     * Authorization header](https://docs.gusto.com/embedded-payroll/docs/system-access)
     * 
     * <p>scope: `payroll_digests:read`
     * 
     * @param security The security details to use for authentication.
     * @param payrollDigestUuid The UUID of the payroll digest batch returned by `POST /v1/payroll_digests`.
     * @param xGustoAPIVersion Determines the date-based API version associated with your API call. If none is provided, your application's [minimum API version](https://docs.gusto.com/embedded-payroll/docs/api-versioning#minimum-api-version) is used.
     * @return {@code CompletableFuture<GetV1PayrollDigestsPayrollDigestUuidResponse>} - The async response
     */
    public CompletableFuture<GetV1PayrollDigestsPayrollDigestUuidResponse> getV1PayrollDigestsPayrollDigestUuid(
            GetV1PayrollDigestsPayrollDigestUuidSecurity security, String payrollDigestUuid,
            Optional<? extends GetV1PayrollDigestsPayrollDigestUuidHeaderXGustoAPIVersion> xGustoAPIVersion) {
        GetV1PayrollDigestsPayrollDigestUuidRequest request =
            GetV1PayrollDigestsPayrollDigestUuidRequest
                .builder()
                .payrollDigestUuid(payrollDigestUuid)
                .xGustoAPIVersion(xGustoAPIVersion)
                .build();
        AsyncRequestOperation<GetV1PayrollDigestsPayrollDigestUuidRequest, GetV1PayrollDigestsPayrollDigestUuidResponse> operation
              = new GetV1PayrollDigestsPayrollDigestUuid.Async(sdkConfiguration, security, _headers);
        return operation.doRequest(request)
            .thenCompose(operation::handleResponse);
    }

}
