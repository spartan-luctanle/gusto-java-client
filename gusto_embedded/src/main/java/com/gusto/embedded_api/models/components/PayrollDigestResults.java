/* 
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
package com.gusto.embedded_api.models.components;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnore;
import com.fasterxml.jackson.annotation.JsonInclude.Include;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.gusto.embedded_api.utils.Utils;
import java.lang.Long;
import java.lang.Override;
import java.lang.String;
import java.lang.SuppressWarnings;
import java.time.OffsetDateTime;
import java.util.List;
import java.util.Optional;
import org.openapitools.jackson.nullable.JsonNullable;

/**
 * PayrollDigestResults
 * 
 * <p>A payroll digest batch with processing results.
 */
public class PayrollDigestResults {
    /**
     * The unique identifier of the payroll digest batch.
     */
    @JsonProperty("uuid")
    private String uuid;

    /**
     * The idempotency key provided when creating the batch.
     */
    @JsonProperty("idempotency_key")
    private String idempotencyKey;

    /**
     * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
     * finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request
     * failed; can be retried). This is distinct from the per-company `status` returned inside `results[]`
     * and `exclusions[]`.
     */
    @JsonProperty("status")
    private PayrollDigestResultsStatus status;

    /**
     * The timestamp when the batch was submitted.
     */
    @JsonProperty("submitted_at")
    private OffsetDateTime submittedAt;

    /**
     * The timestamp when the batch processing completed.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("completed_at")
    private JsonNullable<OffsetDateTime> completedAt;

    /**
     * The number of companies submitted in the batch.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("submitted_items")
    private JsonNullable<Long> submittedItems;

    /**
     * The number of companies successfully processed. Only present once the batch reaches a terminal
     * status.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("processed_items")
    private Optional<Long> processedItems;

    /**
     * The number of companies excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("excluded_items")
    private Optional<Long> excludedItems;

    /**
     * Per-company results. Only present once the batch reaches a terminal status. Includes successfully
     * processed companies (with their `payrolls` array, which may be empty when the company has no
     * payrolls in the date window).
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("results")
    private Optional<? extends List<PayrollDigestResultsResults>> results;

    /**
     * Companies that could not be processed. Only present once the batch reaches a terminal status. Every
     * UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`.
     */
    @JsonInclude(Include.NON_ABSENT)
    @JsonProperty("exclusions")
    private Optional<? extends List<PayrollDigestResultsExclusions>> exclusions;

    @JsonCreator
    public PayrollDigestResults(
            @JsonProperty("uuid") String uuid,
            @JsonProperty("idempotency_key") String idempotencyKey,
            @JsonProperty("status") PayrollDigestResultsStatus status,
            @JsonProperty("submitted_at") OffsetDateTime submittedAt,
            @JsonProperty("completed_at") JsonNullable<OffsetDateTime> completedAt,
            @JsonProperty("submitted_items") JsonNullable<Long> submittedItems,
            @JsonProperty("processed_items") Optional<Long> processedItems,
            @JsonProperty("excluded_items") Optional<Long> excludedItems,
            @JsonProperty("results") Optional<? extends List<PayrollDigestResultsResults>> results,
            @JsonProperty("exclusions") Optional<? extends List<PayrollDigestResultsExclusions>> exclusions) {
        Utils.checkNotNull(uuid, "uuid");
        Utils.checkNotNull(idempotencyKey, "idempotencyKey");
        Utils.checkNotNull(status, "status");
        Utils.checkNotNull(submittedAt, "submittedAt");
        Utils.checkNotNull(completedAt, "completedAt");
        Utils.checkNotNull(submittedItems, "submittedItems");
        Utils.checkNotNull(processedItems, "processedItems");
        Utils.checkNotNull(excludedItems, "excludedItems");
        Utils.checkNotNull(results, "results");
        Utils.checkNotNull(exclusions, "exclusions");
        this.uuid = uuid;
        this.idempotencyKey = idempotencyKey;
        this.status = status;
        this.submittedAt = submittedAt;
        this.completedAt = completedAt;
        this.submittedItems = submittedItems;
        this.processedItems = processedItems;
        this.excludedItems = excludedItems;
        this.results = results;
        this.exclusions = exclusions;
    }
    
    public PayrollDigestResults(
            String uuid,
            String idempotencyKey,
            PayrollDigestResultsStatus status,
            OffsetDateTime submittedAt) {
        this(uuid, idempotencyKey, status,
            submittedAt, JsonNullable.undefined(), JsonNullable.undefined(),
            Optional.empty(), Optional.empty(), Optional.empty(),
            Optional.empty());
    }

    /**
     * The unique identifier of the payroll digest batch.
     */
    @JsonIgnore
    public String uuid() {
        return uuid;
    }

    /**
     * The idempotency key provided when creating the batch.
     */
    @JsonIgnore
    public String idempotencyKey() {
        return idempotencyKey;
    }

    /**
     * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
     * finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request
     * failed; can be retried). This is distinct from the per-company `status` returned inside `results[]`
     * and `exclusions[]`.
     */
    @JsonIgnore
    public PayrollDigestResultsStatus status() {
        return status;
    }

    /**
     * The timestamp when the batch was submitted.
     */
    @JsonIgnore
    public OffsetDateTime submittedAt() {
        return submittedAt;
    }

    /**
     * The timestamp when the batch processing completed.
     */
    @JsonIgnore
    public JsonNullable<OffsetDateTime> completedAt() {
        return completedAt;
    }

    /**
     * The number of companies submitted in the batch.
     */
    @JsonIgnore
    public JsonNullable<Long> submittedItems() {
        return submittedItems;
    }

    /**
     * The number of companies successfully processed. Only present once the batch reaches a terminal
     * status.
     */
    @JsonIgnore
    public Optional<Long> processedItems() {
        return processedItems;
    }

    /**
     * The number of companies excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    @JsonIgnore
    public Optional<Long> excludedItems() {
        return excludedItems;
    }

    /**
     * Per-company results. Only present once the batch reaches a terminal status. Includes successfully
     * processed companies (with their `payrolls` array, which may be empty when the company has no
     * payrolls in the date window).
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<PayrollDigestResultsResults>> results() {
        return (Optional<List<PayrollDigestResultsResults>>) results;
    }

    /**
     * Companies that could not be processed. Only present once the batch reaches a terminal status. Every
     * UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`.
     */
    @SuppressWarnings("unchecked")
    @JsonIgnore
    public Optional<List<PayrollDigestResultsExclusions>> exclusions() {
        return (Optional<List<PayrollDigestResultsExclusions>>) exclusions;
    }

    public static Builder builder() {
        return new Builder();
    }


    /**
     * The unique identifier of the payroll digest batch.
     */
    public PayrollDigestResults withUuid(String uuid) {
        Utils.checkNotNull(uuid, "uuid");
        this.uuid = uuid;
        return this;
    }

    /**
     * The idempotency key provided when creating the batch.
     */
    public PayrollDigestResults withIdempotencyKey(String idempotencyKey) {
        Utils.checkNotNull(idempotencyKey, "idempotencyKey");
        this.idempotencyKey = idempotencyKey;
        return this;
    }

    /**
     * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
     * finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request
     * failed; can be retried). This is distinct from the per-company `status` returned inside `results[]`
     * and `exclusions[]`.
     */
    public PayrollDigestResults withStatus(PayrollDigestResultsStatus status) {
        Utils.checkNotNull(status, "status");
        this.status = status;
        return this;
    }

    /**
     * The timestamp when the batch was submitted.
     */
    public PayrollDigestResults withSubmittedAt(OffsetDateTime submittedAt) {
        Utils.checkNotNull(submittedAt, "submittedAt");
        this.submittedAt = submittedAt;
        return this;
    }

    /**
     * The timestamp when the batch processing completed.
     */
    public PayrollDigestResults withCompletedAt(OffsetDateTime completedAt) {
        Utils.checkNotNull(completedAt, "completedAt");
        this.completedAt = JsonNullable.of(completedAt);
        return this;
    }

    /**
     * The timestamp when the batch processing completed.
     */
    public PayrollDigestResults withCompletedAt(JsonNullable<OffsetDateTime> completedAt) {
        Utils.checkNotNull(completedAt, "completedAt");
        this.completedAt = completedAt;
        return this;
    }

    /**
     * The number of companies submitted in the batch.
     */
    public PayrollDigestResults withSubmittedItems(long submittedItems) {
        Utils.checkNotNull(submittedItems, "submittedItems");
        this.submittedItems = JsonNullable.of(submittedItems);
        return this;
    }

    /**
     * The number of companies submitted in the batch.
     */
    public PayrollDigestResults withSubmittedItems(JsonNullable<Long> submittedItems) {
        Utils.checkNotNull(submittedItems, "submittedItems");
        this.submittedItems = submittedItems;
        return this;
    }

    /**
     * The number of companies successfully processed. Only present once the batch reaches a terminal
     * status.
     */
    public PayrollDigestResults withProcessedItems(long processedItems) {
        Utils.checkNotNull(processedItems, "processedItems");
        this.processedItems = Optional.ofNullable(processedItems);
        return this;
    }


    /**
     * The number of companies successfully processed. Only present once the batch reaches a terminal
     * status.
     */
    public PayrollDigestResults withProcessedItems(Optional<Long> processedItems) {
        Utils.checkNotNull(processedItems, "processedItems");
        this.processedItems = processedItems;
        return this;
    }

    /**
     * The number of companies excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    public PayrollDigestResults withExcludedItems(long excludedItems) {
        Utils.checkNotNull(excludedItems, "excludedItems");
        this.excludedItems = Optional.ofNullable(excludedItems);
        return this;
    }


    /**
     * The number of companies excluded from processing. Only present once the batch reaches a terminal
     * status.
     */
    public PayrollDigestResults withExcludedItems(Optional<Long> excludedItems) {
        Utils.checkNotNull(excludedItems, "excludedItems");
        this.excludedItems = excludedItems;
        return this;
    }

    /**
     * Per-company results. Only present once the batch reaches a terminal status. Includes successfully
     * processed companies (with their `payrolls` array, which may be empty when the company has no
     * payrolls in the date window).
     */
    public PayrollDigestResults withResults(List<PayrollDigestResultsResults> results) {
        Utils.checkNotNull(results, "results");
        this.results = Optional.ofNullable(results);
        return this;
    }


    /**
     * Per-company results. Only present once the batch reaches a terminal status. Includes successfully
     * processed companies (with their `payrolls` array, which may be empty when the company has no
     * payrolls in the date window).
     */
    public PayrollDigestResults withResults(Optional<? extends List<PayrollDigestResultsResults>> results) {
        Utils.checkNotNull(results, "results");
        this.results = results;
        return this;
    }

    /**
     * Companies that could not be processed. Only present once the batch reaches a terminal status. Every
     * UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`.
     */
    public PayrollDigestResults withExclusions(List<PayrollDigestResultsExclusions> exclusions) {
        Utils.checkNotNull(exclusions, "exclusions");
        this.exclusions = Optional.ofNullable(exclusions);
        return this;
    }


    /**
     * Companies that could not be processed. Only present once the batch reaches a terminal status. Every
     * UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`.
     */
    public PayrollDigestResults withExclusions(Optional<? extends List<PayrollDigestResultsExclusions>> exclusions) {
        Utils.checkNotNull(exclusions, "exclusions");
        this.exclusions = exclusions;
        return this;
    }

    @Override
    public boolean equals(java.lang.Object o) {
        if (this == o) {
            return true;
        }
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        PayrollDigestResults other = (PayrollDigestResults) o;
        return 
            Utils.enhancedDeepEquals(this.uuid, other.uuid) &&
            Utils.enhancedDeepEquals(this.idempotencyKey, other.idempotencyKey) &&
            Utils.enhancedDeepEquals(this.status, other.status) &&
            Utils.enhancedDeepEquals(this.submittedAt, other.submittedAt) &&
            Utils.enhancedDeepEquals(this.completedAt, other.completedAt) &&
            Utils.enhancedDeepEquals(this.submittedItems, other.submittedItems) &&
            Utils.enhancedDeepEquals(this.processedItems, other.processedItems) &&
            Utils.enhancedDeepEquals(this.excludedItems, other.excludedItems) &&
            Utils.enhancedDeepEquals(this.results, other.results) &&
            Utils.enhancedDeepEquals(this.exclusions, other.exclusions);
    }
    
    @Override
    public int hashCode() {
        return Utils.enhancedHash(
            uuid, idempotencyKey, status,
            submittedAt, completedAt, submittedItems,
            processedItems, excludedItems, results,
            exclusions);
    }
    
    @Override
    public String toString() {
        return Utils.toString(PayrollDigestResults.class,
                "uuid", uuid,
                "idempotencyKey", idempotencyKey,
                "status", status,
                "submittedAt", submittedAt,
                "completedAt", completedAt,
                "submittedItems", submittedItems,
                "processedItems", processedItems,
                "excludedItems", excludedItems,
                "results", results,
                "exclusions", exclusions);
    }

    @SuppressWarnings("UnusedReturnValue")
    public final static class Builder {

        private String uuid;

        private String idempotencyKey;

        private PayrollDigestResultsStatus status;

        private OffsetDateTime submittedAt;

        private JsonNullable<OffsetDateTime> completedAt = JsonNullable.undefined();

        private JsonNullable<Long> submittedItems = JsonNullable.undefined();

        private Optional<Long> processedItems = Optional.empty();

        private Optional<Long> excludedItems = Optional.empty();

        private Optional<? extends List<PayrollDigestResultsResults>> results = Optional.empty();

        private Optional<? extends List<PayrollDigestResultsExclusions>> exclusions = Optional.empty();

        private Builder() {
          // force use of static builder() method
        }


        /**
         * The unique identifier of the payroll digest batch.
         */
        public Builder uuid(String uuid) {
            Utils.checkNotNull(uuid, "uuid");
            this.uuid = uuid;
            return this;
        }


        /**
         * The idempotency key provided when creating the batch.
         */
        public Builder idempotencyKey(String idempotencyKey) {
            Utils.checkNotNull(idempotencyKey, "idempotencyKey");
            this.idempotencyKey = idempotencyKey;
            return this;
        }


        /**
         * The lifecycle status of the batch request itself. Terminal values are `completed` (processing
         * finished — inspect `results` and `exclusions` for per-company outcomes) and `failed` (request
         * failed; can be retried). This is distinct from the per-company `status` returned inside `results[]`
         * and `exclusions[]`.
         */
        public Builder status(PayrollDigestResultsStatus status) {
            Utils.checkNotNull(status, "status");
            this.status = status;
            return this;
        }


        /**
         * The timestamp when the batch was submitted.
         */
        public Builder submittedAt(OffsetDateTime submittedAt) {
            Utils.checkNotNull(submittedAt, "submittedAt");
            this.submittedAt = submittedAt;
            return this;
        }


        /**
         * The timestamp when the batch processing completed.
         */
        public Builder completedAt(OffsetDateTime completedAt) {
            Utils.checkNotNull(completedAt, "completedAt");
            this.completedAt = JsonNullable.of(completedAt);
            return this;
        }

        /**
         * The timestamp when the batch processing completed.
         */
        public Builder completedAt(JsonNullable<OffsetDateTime> completedAt) {
            Utils.checkNotNull(completedAt, "completedAt");
            this.completedAt = completedAt;
            return this;
        }


        /**
         * The number of companies submitted in the batch.
         */
        public Builder submittedItems(long submittedItems) {
            Utils.checkNotNull(submittedItems, "submittedItems");
            this.submittedItems = JsonNullable.of(submittedItems);
            return this;
        }

        /**
         * The number of companies submitted in the batch.
         */
        public Builder submittedItems(JsonNullable<Long> submittedItems) {
            Utils.checkNotNull(submittedItems, "submittedItems");
            this.submittedItems = submittedItems;
            return this;
        }


        /**
         * The number of companies successfully processed. Only present once the batch reaches a terminal
         * status.
         */
        public Builder processedItems(long processedItems) {
            Utils.checkNotNull(processedItems, "processedItems");
            this.processedItems = Optional.ofNullable(processedItems);
            return this;
        }

        /**
         * The number of companies successfully processed. Only present once the batch reaches a terminal
         * status.
         */
        public Builder processedItems(Optional<Long> processedItems) {
            Utils.checkNotNull(processedItems, "processedItems");
            this.processedItems = processedItems;
            return this;
        }


        /**
         * The number of companies excluded from processing. Only present once the batch reaches a terminal
         * status.
         */
        public Builder excludedItems(long excludedItems) {
            Utils.checkNotNull(excludedItems, "excludedItems");
            this.excludedItems = Optional.ofNullable(excludedItems);
            return this;
        }

        /**
         * The number of companies excluded from processing. Only present once the batch reaches a terminal
         * status.
         */
        public Builder excludedItems(Optional<Long> excludedItems) {
            Utils.checkNotNull(excludedItems, "excludedItems");
            this.excludedItems = excludedItems;
            return this;
        }


        /**
         * Per-company results. Only present once the batch reaches a terminal status. Includes successfully
         * processed companies (with their `payrolls` array, which may be empty when the company has no
         * payrolls in the date window).
         */
        public Builder results(List<PayrollDigestResultsResults> results) {
            Utils.checkNotNull(results, "results");
            this.results = Optional.ofNullable(results);
            return this;
        }

        /**
         * Per-company results. Only present once the batch reaches a terminal status. Includes successfully
         * processed companies (with their `payrolls` array, which may be empty when the company has no
         * payrolls in the date window).
         */
        public Builder results(Optional<? extends List<PayrollDigestResultsResults>> results) {
            Utils.checkNotNull(results, "results");
            this.results = results;
            return this;
        }


        /**
         * Companies that could not be processed. Only present once the batch reaches a terminal status. Every
         * UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`.
         */
        public Builder exclusions(List<PayrollDigestResultsExclusions> exclusions) {
            Utils.checkNotNull(exclusions, "exclusions");
            this.exclusions = Optional.ofNullable(exclusions);
            return this;
        }

        /**
         * Companies that could not be processed. Only present once the batch reaches a terminal status. Every
         * UUID submitted in the POST batch appears in exactly one of `results` or `exclusions`.
         */
        public Builder exclusions(Optional<? extends List<PayrollDigestResultsExclusions>> exclusions) {
            Utils.checkNotNull(exclusions, "exclusions");
            this.exclusions = exclusions;
            return this;
        }

        public PayrollDigestResults build() {

            return new PayrollDigestResults(
                uuid, idempotencyKey, status,
                submittedAt, completedAt, submittedItems,
                processedItems, excludedItems, results,
                exclusions);
        }

    }
}
